## 经济数据的类型
1. 横截面（Cross-sectional）数据： 多个**不同**经济个体、**同一**时间点
2. 时间序列（Time Series）数据： **同一**经济个体、**不同**时间点
3. 面板（Panel）数据：多个**不同**经济个体、**不同**时间点

基础展示
```
describe
list x
sum x
```
绘图
```
 twoway (kdensity x) || (kdensity y if 判断语句)
```

## 一元最优化的一阶条件
$f'(x)=0$（一阶导数，即切线斜率为0），又称**一阶条件**（First  Order  Condition）
## 二次型是什么 
$x'Ax$可视为向量x的**加权平方和**，衡量**向量x**与**零向量0**的距离
## 偏度
三阶中心矩
## 峰度
四阶中心矩
## 超额峰度
正态分布的峰度可作为判断其它分布的基准，因此记**超额峰度**(excess   kurtosis)为 $E\left[\frac{(X - \mu)}{\sigma}\right]^4-3$
## 随机变量无关的三个层次
**相互独立**($f(x,y) = f_X(x)f_Y(y)$) $\implies$ **均值独立**($E(Y|X) = E(Y)$) $\implies$ **线性不相关** ($Cov(X,Y)=0$)
## 卡方分布
$Y = \sum_{i=1}^k Z_i^2 \sim \chi^2(k)$ 其中：$Z_1,..,Z_k \sim N(0,1)$
 当k=1时: $Z^2 \sim \chi^2(1)$

 ## t分布
 $T = \frac{Z}{\sqrt{Y/k}} \sim t(k)$其中：$Z \sim N(0,1)$, $Y \sim \chi^2(k)$，且Z与Y相互独立
 
 ## F分布
 $F = \frac{Y_1/k_1}{Y_2/k_2} \sim F(k_1, k_2)$其中： $Y_1 \sim \chi^2(k_1)$, $Y_2 \sim \chi^2(k_2)$, 且$Y_1, Y_2$相互独立

## T分布与F分布的关系
t分布的平方是分子为正态的F分布，即：$X$~$t(k)$ => $X^2$~$F(1, k)$

## 什么是偏差/无偏估计
如果 $E(\hat{\theta}) = \theta$，即**偏差为0**，则称 $\hat{\theta}$ 是 $\theta$ 的**无偏估计量** (Unbiased Estimator)，反之则为**有偏估计**(biased estimator)

## MSE是什么，可以分解为什么
**均方误差MSE**(Mean Squared Error)：$$MSE(\hat{\theta}) = E [ (\hat{\theta} - \theta)^2 ]$$可以证明，它同时取决于**方差**（衡量估计量围绕其均值的波动性）和**偏差的平方**（衡量估计量均值偏离真值的程度），即：
$$ MSE(\hat{\theta}) = Var(\hat{\theta}) + [Bias(\hat{\theta})]^2 $$
## OLS是什么
 通过**最小化残差平方和** (Sum of Squared Residuals, SSR)来估计参数

## OLS的一阶条件

$$
\begin{cases}
\frac{\partial}{\partial \hat{\alpha}} \sum_{i=1}^n e_i^2 = 0 \\
\\
\frac{\partial}{\partial \hat{\beta}} \sum_{i=1}^n e_i^2 = 0
\end{cases}
$$
 ## OLS的β估计公式
 $$\hat{\beta} = \frac{\sum_{i=1}^n (x_i - \bar{x})(y_i - \bar{y})}{\sum_{i=1}^n (x_i - \bar{x})^2} = \frac{\sum_{i=1}^n x_i y_i - n \bar{x} \bar{y}}{\sum_{i=1}^n x_i^2 - n \bar{x}^2}$$
![[250421-考点汇总-1.png]]
## 样本回归线一定经过？
**样本回归线**(sample regression line)一定经过均值点 ( $\bar{x}$ , $\bar{y}$ )

## R平方是什么
我们可以定义一个衡量模型**拟合优度**(goodness of fit)的指标，又为 **可决系数** (Coefficient of Determination) 或 **R 方 ($R^2$)**：
$$ R^2 = \frac{ESS}{TSS} = 1 - \frac{SSR}{TSS} $$
![[250421-考点汇总-2.png]]


 ## 一元回归Stata指令
 ```
* noc参数指定无常数项
* reg lnw s, noc

* 绘制散点图-线性拟合图
twoway scatter child parent || lfit child parent

reg lnw s expr tenure smsa rns if s>=12

reg lnw s expr tenure smsa if s>=12 & rns

*计算拟合值并记为lnw1
predict lnw1

predict e,residual

*检验
test s=0.1

*检验联合显著性 
test rain coast
```

## 多元/一元回归
| 类型     | 被解释变量/因变量（$y$） | 解释变量/自变量（$x$） |
| ------ | -------------- | ------------- |
| 一元线性回归 | 1个             | 1个            |
| 多元线性回归 | 1个             | **多个**        |

## 第1/2类错误
1. **第Ⅰ类错误**(Type error Ⅰ )：原假设为真，但**拒绝**了原假设（"弃真"）
2. **第ⅠⅠ类错误**(Type error ⅠⅠ )：原假设为假，但**接受**了原假设（"存伪"）

## 三个收敛
- 随机变量 $X_n$ **依概率收敛 (Convergence in Probability)** 于常数 $a$ (或随机变量 $X$)， 记作 $X_n \xrightarrow{p} a$ （或 $X_n \xrightarrow{p} X$）
- 随机变量 $X_n$ **依均方收敛** (Convergence in Mean Square)于常数 $a$ ，记作 $X_n \xrightarrow{ms} a$
- 随机变量 $X_n$**依分布收敛** (Convergence in Distribution)于随机变量 $X$ ，记作 $X_n \xrightarrow{d} X$

$$
依均方收敛 \implies 依概率收敛 \implies依分布收敛
$$

## 两大定律
- 对于**独立同分布**的随机序列 $\{x_n\}_{n=1}^\infty$ ，其样本均值 $\bar{x}_n = \frac{1}{n}\sum_{i=1}^n x_i$ **依概率收敛**于总体均值 $\mu$。
- 对于**独立同分布**的随机序列 $\{x_n\}_{n=1}^\infty$ ，其标准化后的样本均值 $\bar{x}_n = \frac{1}{n}\sum_{i=1}^n x_i$ **依概率收敛**于标准正态分布：$$ \frac{\bar{x}_n - \mu}{\sqrt{\sigma^2/n}} \xrightarrow{d} N(0, 1) $$

## 渐近性质
- **一致性 (Consistency)**：当样本容量 $n$ 趋于无穷大时，OLS 估计量 $\hat{\beta}_n$ 会**依概率收敛**到真实的总体参数 $\beta$
- **渐近正态性 (Asymptotic Normality)**：当样本容量 $n$ 趋于无穷大时，经过适当标准化后的 OLS 估计量 $\hat{\beta}_n$ 的**分布**会趋近于正态分布。即：$$ \sqrt{n}(\hat{\beta}_n - \beta) \xrightarrow{d} N(0, \sigma^2) $$
- **渐近独立定理**：如果随机序列 $\{x_i\}_{i=1}^\infty$ 是渐近独立的，那么其**函数** $y_i = f(x_i)$ 构成的序列 $\{y_i\}_{i=1}^\infty$ 也是渐近独立的

| 所需假设前提   | 小样本OLS                        | 大样本OLS                         |
| -------- | ----------------------------- | ------------------------------ |
| 方程形式     | 线性性（线性假定）                     | 线性性（线性假定）                      |
|          |                               | 渐近独立的平稳过程（所有变量渐近独立）            |
| 解释变量外生性质 | **严格外生性**（解释变量与**所有观测值**均不相关） | **前定解释变量**（解释变量与**同期扰动项**均不相关） |
| 变量间多重共线性 | 秩条件（不存在多重共线性）                 | 秩条件（不存在多重共线性）                  |
| 扰动项性质    | **球形扰动项**（扰动项同方差且不相关）         |                                |

| 所具备的性质 | 小样本OLS            | 大样本OLS                |
| ------ | ----------------- | --------------------- |
| 1      |                   | 一致性（估计参数依概率收敛到真实参数）   |
| 2      |                   | 渐近正态性（估计量标准化后的分布趋近正态） |
| 3      | 无偏性（估计参数均值等于真实参数） |                       |
| 4      | 有效性（估计量是最佳线性无偏估计） |                       |

## 异方差/自相关的后果

1. OLS 估计量 $\hat{\beta}$ 仍满足**无偏性**、**一致性**、**渐近正态**（推导不依赖同方差）
2. OLS **标准误失效**：导致基于普通标准误的 t 检验、F 检验 不可靠。
3. OLS  **不再是最佳线性无偏估计**（有效性）：高斯-马尔可夫定理不再成立，OLS 不再是最高效的线性无偏估计量 。

## BP检验
```Stata
estat hettest [指定变量，空则为全部]
```
## 怀特检验
```Stata
estat imtest, white
```

## BG检验
```Stata
estat bgodfrey 
```

## Q检验
```Stata
wntestq e 
```

## DW检验
```Stata
estat dwatson
```

## HAC检验
```Stata
newey y x1 x2 ... xk, lag(p)
```


```
*存在异方差，使用稳健标准误方法
reg ..., r


* 滞后项L1.变量 L2.变量；L（1/2）.变量 代表一阶、二阶滞后项
* 加入气温的一阶滞后项
reg consumption temp L.temp price income
reg consumption temp L.temp L2.temp price income


* (1) 绘制food_exp与income的散点图、线性拟合图
twoway (scatter food_exp income)||(lfit food_exp income)

* (2) 计算残差，记为e1
predict e1, resid

* 绘制残差与前一期残差的散点图
gen e1_lag = e1[_n-1]
twoway (scatter e1 e1_lag)||(lfit e1 e1_lag)

* (3) 绘制残差的自相关图
ac e1
```

## 遗漏变量的情况
1. 遗漏变量与解释变量**不相关**：=>遗漏变量被归入扰动项中 =>影响OLS估计的准确度
2. 遗漏变量与解释变量**相关**：=>导致"遗漏变量偏差" (omitted variable bias) -> 是"致命伤"，需要尽量避免

## 信息准则

1. **AIC**"赤池信息准则"(Akaike Information Criterion)：选择解释变量的个数 K，使得目标函数最小化：$$
   \min_K \text{AIC} = \ln\left(\frac{\text{SSR}}{n}\right) + \frac{2}{n}K
$$
2. **BIC**"贝叶斯信息准则"(Bayesian Information Criterion) 或 "施瓦茨信息准则"(Schwarz Information Criterion, SIC 或 SBIC)：选择解释变量的个数 K，使得目标函数最小化：$$
   \min_K \text{BIC} = \ln\left(\frac{\text{SSR}}{n}\right) + \frac{\ln n}{n}K
$$
![[250421-考点汇总-3.png]]

## 多重共线性
某个变量的方差膨胀因子（VIF）越大， 其**多重共线性问题越严重**，其**方差越大**。

## 虚拟变量
如果定性指标共分M类，则最多只能在方程中放入**M-1**个虚拟变量，否则将导致**多重共线性**。

## 工具变量法的原理
工具变量满足以下两个条件：
1. **相关性**(relevance)：工具变量与**内生解释变量**相关
2. **外生性**(exogeneity)：工具变量与**扰动项**不相关

常见的工具变量例子有：
1. 地理位置：研究受教育程度对工资的影响‌时，住在大学附近可能会影响一个人的受教育程度（一个内生解释变量），但不会直接影响工资水平、不与扰动项相关
2. 母亲的教育年限med、测试成绩kww：研究教育投资回报率时，母亲教育年限、测试成绩与智商iq（一个内生解释变量）相关，但不与扰动项相关
3. 降雨量：研究看电视是否引发小儿自闭症时，降雨越多的地区，人们呆在室内时间越长，看电视时间也越长， 故其与看电视相关，但不直接影响小儿自闭症的产生

![[250421-考点汇总-4.png]]

## 过度识别检验
```
 estat overid
```

![[250421-考点汇总-5.png]]

## 边际效应
```
margins,dydx(*)
```


1.  **固定效应 (Fixed Effects, FE)**：
    *   **核心假设**: 个体效应 $a_i$ 与解释变量 $x_{it}$ **相关**
2.  **随机效应 (Random Effects, RE)**：
    *   **核心假设**:  个体效应 $a_i$ 与解释变量 $x_{it}$ **不相关**