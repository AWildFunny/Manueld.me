分类：
1. 两个序列：维护某种次序（比如**归并排序**中合并两个有序序列的操作）
2. 单个序列：用两个指针维护一段区间（如找出符合某一条件的子序列）

核心用途：遍历的损耗从$O(n^2)$ 简化到$O(n)$

```cpp
for (int i = 0, j = 0; i < n; i ++ )
{
    while (j < i && check(i, j)) j ++ ;

    // 具体问题的逻辑
}
```