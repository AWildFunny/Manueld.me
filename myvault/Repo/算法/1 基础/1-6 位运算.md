## 1. 求整数x的第k位数字

```cpp
x >> k & 1
```
1. 右移k位：使得第k位为个位
2. 取出个位：即与1（二进制000...001）做&运算，从而只保留个位数
## 2. 求整数x的最低位1
```cpp
lowbit(x) = x & ~x = x & (~x+1)
```
-  代表二进制数中：最低位1及其后续0组成的数（如110->10、1100->100）
- 应用：求x中二进制1的个数（每次减去最后一位1，所用的次数记为所求）