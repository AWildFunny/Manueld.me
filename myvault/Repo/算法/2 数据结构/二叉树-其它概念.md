## 线索二叉树
>目的：加快查找结点前驱或后继的速度
- 使用链表存储n个结点的二叉树，会有n+1个空指针 -> 引入线索二叉树：使用这些空指针存储其前驱/后继 -> 加快查找速度
- 实现方式：
	- 无左子树的结点：空指针lchild -> 指向前驱结点
	- 无右子树的结点：空指针rchild -> 指向后继结点
	- 每个结点新增ltag/rtag =>  代表该结点是否采用上述规则填充空指针的空间：为1时启用上述规则，为0时不启用上述规则
- 前序/中序/后序线索树：按前序/中序/后序遍历时得到的顺序即为调整后的指针顺序